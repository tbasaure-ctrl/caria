# Kubernetes Manifests

- `deployment-mcp.yaml`: despliegue del servidor MCP con autoescalado.
- `deployment-api.yaml`: despliegue de la API analítica.
- `cron-backfill.yaml`: job programado para refrescar embeddings.

Los manifiestos se generarán posteriormente usando Helm o Kustomize.

